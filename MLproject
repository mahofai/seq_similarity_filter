name: autoML

conda_env: conda.yml

entry_points:
  main:
    parameters:
      alpha: {type: float, default: 0.5}
      l1_ratio: {type: float, default: 0.1}
    command: "python autogluon-mlflow-project.py {alpha} {l1_ratio}"
